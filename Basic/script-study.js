const coffeeMenu = [];

function displayMenu() {}

let selectedCoffees = [];

function selectCoffee(index) {}

function showSelection() {}

// Event listener for the button
// When the button is clicked, the showSelection function is called
// The showSelection function displays the selected coffees
// addEventListener takes 2 arguments:
// 1. The event type ("click")
// 2. The event handler function (showSelection)
document
  .getElementById("show-selection")
  .addEventListener("click", showSelection);

// Display the menu on page load
displayMenu();
